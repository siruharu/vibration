# 음향/진동 분석 프로그램 최적화 모듈
# Python 3.8 이상 필요

# GUI 프레임워크
PyQt5>=5.15.0

# 핵심 계산 라이브러리
numpy>=1.21.0
scipy>=1.7.0

# 시각화
matplotlib>=3.4.0

# 데이터 처리
pandas>=1.3.0

# 오디오 파일 처리
soundfile>=0.10.3
librosa>=0.9.0

# TDMS 파일 지원 (LabVIEW)
nptdms>=1.3.0

# 병렬 처리 (선택사항, 기본 포함)
# concurrent.futures는 Python 표준 라이브러리

# 로깅 및 유틸리티 (표준 라이브러리)
# logging, pathlib, platform은 Python 기본 포함

# PyInstaller (배포용, 선택사항)
# pyinstaller>=5.0.0

# 개발 도구 (선택사항)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=4.0.0

python-qt5